type Query {
  user: User
}

type Mutation {
  user: UserMutations
}

type UserMutations {
  logIn(username: String!, password: String!): SessionId
}

type User {
  realname: String
  email: String
  level: Int
}
