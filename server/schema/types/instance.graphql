type Query {
  instance(projectId: String!, instanceId: String!): Instance
  instanceCollection(projectId: String!): [Instance]
}

type Instance {
  # Instance name
  name: String
  # Instance id
  id: String
  # Instance flavor id
  flavorId: String
  # Instance region
  region: String
}
